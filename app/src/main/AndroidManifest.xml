<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adige.cahillerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragments.homepage" />
        <activity android:name=".fragments.mylist" />
        <activity android:name=".fragments.cloud" />
        <activity android:name=".fragments.navigationDrawer" />
        <activity android:name=".navigationFragments.interest" />
        <activity android:name=".navigationFragments.settings" />
        <activity android:name=".user.loginpage" />
        <activity android:name=".user.registerpage"></activity>
    </application>

</manifest>